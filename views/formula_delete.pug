extends layout

block content
  h1.li4 #{title} : #{formula.name}
  p Do you want to delete this formula?
  div.form-group
    form(method='POST' action='')
      input#formula.form-content(type='hidden' name='formulaid' value=formula._id)
      button.btn.btn-primary(type='submit') Delete
