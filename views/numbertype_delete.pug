extends layout

block content
  h1.li5 #{title} : #{numbertype.name}
  p Do you want to delete this Number Type?
  div.form-group
    form(method='POST' action='')
      input#numbertype.form-content(type='hidden' name='numbertypeid' value=numbertype._id)
      button.btn.btn-primary(type='submit') Delete
