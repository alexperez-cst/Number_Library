extends layout

block content
  h1.li2 #{title} : #{number.name}
  p Do you want to delete this number?
  div.form-group
    form(method='POST' action='')
      input#number.form-content(type='hidden' name='numberid' value=number._id)
      button.btn.btn-primary(type='submit') Delete
